% ============================================================
%  Chapter 2: Background
% ============================================================
%
%  TARGET: ~700 words.
%  what does a reader minimally need to follow Chapters 3-5?
%
%  HARD RULE: assume a reader with strong ML background but no
%  CT-specific knowledge. They don't need derivations -- they
%  need the model y = Ax + eta and an intuition for why
%  inversion is hard.
%
% ============================================================
%
%  KEEP from current draft:
%   - Forward model equation (eq:forward-model)
%   - One-line Radon transform definition + citation
%   - Sparse-view + streak-artefact paragraph (compressed)
%   - Brief noise model (Poisson -> Gaussian after log)
%
%  CUT from current draft:
%   - Full Beer-Lambert derivation (one sentence, cite)
%   - Central Slice Theorem (one sentence, cite)
%   - Hadamard conditions paragraph (one sentence)
%   - SVD/Hansen discussion (one sentence, cite)
%   - Limited-angle CT paragraph (out of scope)
%   - Full FBP two-stage derivation (compress to 2 sentences)
%   - SIRT subsection (mention in passing only)
%   - "From classical to learned" subsection (cut entirely;
%     Chapter 3 opens with this)
%   - Remark on LION/tomosipo (move to Chapter 4)
%
% ============================================================
%
%  TARGET STRUCTURE (3 sections, no subsections):
%
%  2.1 The sparse-view CT inverse problem    ~350 w
%       ¶ Beer-Lambert in 1 sentence; Radon transform in 1
%         sentence with citation; the discrete forward model
%         y = Ax + eta as the working model; A is the
%         GPU-evaluated system operator (no dense storage).
%       ¶ Why sparse views are hard: undersampled angles ->
%         non-trivial null space of A -> streak artefacts; cite
%         Hadamard ill-posedness; mention SVD decay of A in 1
%         sentence.
%       ¶ Noise model: Poisson photon counts -> approximately
%             Gaussian after log-transform; the three noise
%             levels used by TFPnP (5/7.5/10%) define the
%             experimental regime.
%
%  2.2 Classical reconstruction              ~250 w
%       ¶ FBP: ramp filter + backprojection; analytically exact
%         under full sampling, fails under sparse views.
%       ¶ TV-regularised ADMM: piecewise-constant prior;
%         introduce ADMM as the framework that splits data
%         fidelity from the prior, motivating the PnP
%         generalisation in Ch.3. Cite Rudin-Osher-Fatemi and
%         Sidky-Pan.
%
%  2.3 Plug-and-Play priors                  ~100 w
%       ¶ The PnP insight: the x-update of ADMM is a
%         denoising step; replace it with any denoiser
%         (Venkatakrishnan 2013). State PnP-ADMM in three
%         equations. End with the manual tuning problem
%         (sigma_k, mu_k, termination) -- this motivates
%         Chapter 3.
%
% ============================================================
%  FIGURES (1 figure max for this chapter, captions count!)
%
%  Fig 2.1: Sparse-view artefacts demonstration.
%      Source: figures/02/03_fbp_sparse_view.pdf
%      Composite showing FBP from 720 views vs FBP from 30 views
%      on a sample LIDC slice. Caption ~35 words.
%
%  (OPTIONAL Fig 2.2: TV vs FBP at 30 views, but only if word
%   budget allows. figures/02/04_streak_artefacts.pdf or similar.
%   Most likely CUT.)
%
% ============================================================
%  CITATIONS REQUIRED (already in current draft, retain):
%   Radon1917, Natterer2001 (for Radon transform definition)
%   RudinOsherFatemi1992, SidkyPan2008 (for TV)
%   Buzug2008 (for Poisson noise model)
%   Hansen1992, Hansen1998 (for ill-posedness, 1 cite suffices)
%   Wei2022TFPnP (for noise-level convention)
%   Venkatakrishnan2013 (for PnP, in 2.3)
%   Boyd2011 (for ADMM, in 2.2)
% ============================================================

\chapter{Background}
\label{chap:background}

% This chapter establishes the forward model y = Ax + eta and the
% PnP-ADMM iteration that Chapter 3 will extend with a learned policy.

% -----------------------------------------------------------
\section{The Sparse-View CT Inverse Problem}
\label{sec:ct-basics}
% -----------------------------------------------------------

% TODO ¶1 [Beer-Lambert + Radon + discrete model, ~150 w]
% Open with Beer-Lambert in one sentence: a ray of initial intensity
% I_0 traversing a medium with attenuation mu emerges with intensity
% I = I_0 exp(-int_L mu dl), so p = -ln(I/I_0) is a line integral of mu.
% The Radon transform R generalises this to all rays (cite Radon1917,
% Natterer2001) -- one sentence, no equation.
% Then the WORKING equation:

\begin{equation}
    \bm{y} = A\bm{x} + \bm{\eta},
    \label{eq:forward-model}
\end{equation}

% with one sentence defining x (vectorised image), y (stacked sinogram),
% A (system matrix, never stored explicitly).

% TODO ¶2 [ill-posedness, ~120 w]
% Sparse-view CT undersamples angles -> A has a non-trivial null space
% -> infinitely many x consistent with y -> streak artefacts under naive
% inversion (cite Hadamard1923 for the formal sense of ill-posedness).
% Add a single sentence on SVD decay (cite Hansen1998).
% Reference Fig 2.1 here.

% TODO ¶3 [noise model, ~80 w]
% Poisson photon counts at the detector become approximately Gaussian
% in the log-domain (cite Buzug2008). The TFPnP paper (cite
% Wei2022TFPnP) tests Gaussian noise at sigma in {5%, 7.5%, 10%} of the
% sinogram max; the present project follows this convention.

% Insert Fig 2.1 here -- sparse-view artefacts
% \begin{figure}[htbp]
%     \centering
%     \includegraphics[width=\textwidth]{figures/02/03_fbp_sparse_view.pdf}
%     \caption{Filtered backprojection of a representative LIDC-IDRI
%     slice from 720 projection angles (left, clinical reference) and
%     from 30 angles (right, the regime of this work). The 24$\times$
%     dose reduction produces severe streak artefacts that motivate
%     learned reconstruction.}
%     \label{fig:sparse-artefacts}
% \end{figure}


% -----------------------------------------------------------
\section{Classical Reconstruction}
\label{sec:classical-reconstruction}
% -----------------------------------------------------------

% TODO ¶1 [FBP, ~80 w]
% Filtered backprojection: filter each projection with a ramp kernel
% then backproject; analytically exact under dense noiseless sampling.
% Under sparse views the ramp amplifies high-frequency noise and the
% missing-angle null space produces streaks (visible in Fig 2.1).

% TODO ¶2 [TV + ADMM as framework, ~150 w]
% TV-regularised reconstruction (cite RudinOsherFatemi1992, SidkyPan2008):
%     argmin_x (1/2) ||Ax - y||^2 + lambda || grad x ||_1
% The non-smooth regulariser motivates proximal methods; ADMM
% (cite Boyd2011) is the standard tool. Write the three ADMM updates
% in compact form (or in a small numbered block) to set up the PnP
% generalisation in Section 2.3 -- this is the lone "real" maths
% block of the chapter.


% -----------------------------------------------------------
\section{Plug-and-Play Priors}
\label{sec:pnp}
% -----------------------------------------------------------

% TODO ¶1 [PnP insight, ~70 w]
% The image-update in ADMM is the proximal operator of R applied to
% (z - u); this is itself a denoising step (cite Venkatakrishnan2013).
% PnP replaces it with any off-the-shelf denoiser H_sigma:
%     x^(k+1) = H_sigma_k (z^k - u^k)
%     z^(k+1) = (mu A^T A + I)^-1 (mu A^T y + x^(k+1) + u^k)
%     u^(k+1) = u^k + x^(k+1) - z^(k+1)

% TODO ¶2 [the tuning problem, ~50 w]
% PnP-ADMM is highly sensitive to the schedule (sigma_k, mu_k) and the
% termination time tau. Optimal schedules are image-dependent and not
% obtained by hand-crafted heuristics -- this is precisely the gap that
% TFPnP closes (Chapter 3).

% End-of-chapter bridge sentence (~20 w):
% "We now turn to learned approaches that automate this schedule."